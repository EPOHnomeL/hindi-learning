# 05 — Landing page → next-intl

Wire `src/app/_components/Landing.tsx`. New namespace `Landing`.

Cover: STEPS (3 titles+bodies); FEATURES (6 titles+bodies); ThemeToggle aria/title
(reuse Common); hero (Sign in, eyebrow, headline with `<em>` via `t.rich`, subhead,
Get started, See how it works); section headings + bodies; Certificates block.

`DEMO_CERT` sample data (name/course) may stay as demo content — flag only, do not
block.

Done when: landing renders in the active locale; keys in en.json; tsc clean.
