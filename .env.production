# Loaded by Vite for `pnpm build` only (production mode). The Neon Auth base URL
# is public — it ships to the browser — so this file is committed. Local dev
# (`pnpm client`) does not load it, so the dev reader stays gate-free and the
# worker falls back to the dev user.
VITE_NEON_AUTH_URL="https://ep-young-bread-as4yo5g5.neonauth.c-4.eu-central-1.aws.neon.tech/neondb/auth"
